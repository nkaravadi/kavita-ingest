# Copy this file to .env and edit as needed.
# All of these are optional — you can configure everything through the /settings UI instead.

# Listening port (default: 8080)
PORT=8080

# Seed values written to data/settings.json on very first run only.
# Leave blank to configure via the UI after first start.
KAVITA_URL=
KAVITA_API_KEY=
